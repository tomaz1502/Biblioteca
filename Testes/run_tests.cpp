#include "DP/dp_tests.cpp"
#include "minunit.h"

int main(int argc, char* argv[]) {
  /* vector<string> args; */
  /* for (int i = 1; i < argc; i++) { */
  /*   args.emplace_back(argv[i]); */
  /* } */

  /* for (const auto& arg: args) { */
  /*   if (arg == "all") { */

  /*   } */
  /* } */

  MU_RUN_TEST(DP);
  MU_REPORT();

  return 0;
}
